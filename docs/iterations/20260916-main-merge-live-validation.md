# main 合并后真实功能验证

测试版本：`ca1c3e4`。日期：2026-09-16。没有修改产品源码、题目或预期，也没有为提高成绩重跑失败题。

结论：主要问答及界面操作可用，但不能宣称全部功能通过。真实问题集有语义失败；原生 Computer Use 截图被环境阻断，浏览器联调不能代替全部原生验收。

## Computer Use 实测

原生应用路径为本仓库的 `build/bin/HDU Station.app`。多次获取窗口均返回 `screen capture failed (-10005)`。机器另有同 bundle ID 的 `hdu-station-zero` 应用，因此采用完整路径，没有把另一个仓库的应用当成本次结果。

后续通过 Computer Use 实际点击、输入、滚动和键盘操作本版本 Wails 开发入口 `127.0.0.1:34116`；前端为已构建资源，绑定真实 Go 后端、模型、校园接口和社区来源，无 mock。使用应用日志目录内的隔离数据目录，不复制用户历史。测试结束已停止开发进程、恢复浏览器尺寸并清除临时复制的凭证及工具文件，保留测试问答数据库。

| 功能 | 结果与证据 |
| --- | --- |
| 首页、中文输入、发送 | 通过；真实查询影视音乐赏析开课，返回三个班次 |
| 首页推荐快捷入口 | 通过；生成真实 QQ/小红书社区推荐与原帖链接 |
| 本人课表 | 通过；实际查询周二下午，显示局部占用与空闲 |
| 并行对话 | 通过；社区推荐在后台继续，前台独立查询课表，显示另有一个对话运行 |
| 停止与重新回答 | 通过；社区请求停止后保留部分正文，显示停止状态；重新回答可启动并再次停止 |
| 历史切换与持久化 | 通过；可切回真实教务表格；刷新后四条测试通讯仍在 |
| 四种配色 | 通过；暖阳田园、雾白青瓷、灰紫青绿、青蓝琥珀均可切换并检查可读性 |
| 非默认配色持久化 | 通过；重新加载完成后仍为雾白青瓷；原生启动防闪烁未验证 |
| 逐字显示偏好 | 开关保存通过；重新打开设置保留关闭状态，随后恢复开启。完整逐字动画与立即显示未在本轮单独完成验收 |
| 校园登录状态 | 通过；读取已登录状态，刷新后显示状态已更新 |
| QQ/小红书来源状态 | 通过；设置显示真实连接状态，QQ重新检查成功 |
| QQ启用开关 | 通过；关闭后显示未启用且保留凭证，再次启用后恢复已连接 |
| 保存并检查 | 通过；空闲时保存成功，生成期间提示等回答结束后再修改设置 |
| 桌面尺寸表格 | 通过；真实开课表、来源说明及输入区可读 |
| 390×844窄窗口 | 通过；表格可横向滚动到结果列，输入框和设置可访问 |
| 窄窗口历史抽屉 | 通过；打开、关闭和 Escape 恢复菜单焦点正常 |
| 快捷键 | 通过；Command+K 聚焦新对话输入，Command+, 打开设置 |
| 删除确认 | 确认和取消通过；点击保留后记录仍在。未执行最终删除 |
| 复制回答 | 点击未显示错误，但浏览器虚拟剪贴板无法读取 Wails 系统剪贴板，端到端复制待原生核验 |
| 外部原帖打开 | 链接渲染通过；未完成宿主默认浏览器打开及小红书签名补全验收 |
| 重新授权、扫码、退出、清除凭证、安全验证码 | 未执行完整流程；涉及现有共享账号状态及人工扫码，不计为通过 |
| 赞哦 | 当前未启用且没有凭证；设置入口可展开，真实搜索未验证 |

开发后端出现三次 `Unknown message from front end: runtime:ready` 日志；本轮绑定、问答和设置仍正常工作。该现象限定为开发联调观察，不据此判定生产原生包故障。重新加载初期会出现默认主题/连接加载态，生产包启动主题注入路径仍需原生检查。

此前同一合并版本的 `make test`、`make frontend-check`（由 test/build 调用）、`make build` 以及28项桌面/窄窗口自动化测试已通过。本轮 Computer Use 与它们分别记录，不将自动化替身测试计作真实登录验收。

## 真实问题集

使用原53组、58轮题集，3路并发，运行一次，耗时555.79秒。生产 Engine 使用已保存的真实模型与连接。测试进程 PASS 仅表示执行完成，不代表语义正确。

| 指标 | 结果 |
| --- | ---: |
| 通过 | 38组 |
| 部分完成 | 8组 |
| 失败 | 7组 |
| 请求错误 / 空回答 | 0 / 0 |
| 自动范围标记 | 2次 |
| 单轮耗时中位数 / 最长 | 7.8秒 / 143.9秒 |

逐题按可见回答、脱敏工具操作名和题目预期人工审核；没有重新打开全部原帖独立验真，来源限定也未对全部请求单独抓包。QQ连接预检16次就绪，小红书13次就绪、3次不可用；赞哦未启用。本轮并行进行了少量真实界面问答，因此耗时不宜直接与此前独占运行作性能对比。

### 七组失败

| 题目 | 问题 |
| --- | --- |
| R03 | 把“周一下午已有课”误当允许时段，遗漏当天晚间可放班 |
| E18 | 只列开课表，漏答同一课程不能按三个班凑六学分 |
| E23 | 只要求开课时间却读取本人课表，触发 `unrequested_timetable` |
| E24 | 未应用周二/周四筛选，且额外查社区，触发 `unrequested_community` |
| E53 | 只给社区候选和本人课表，未完成候选开课及组合适配 |
| E54 | 未澄清“只要周五且周五全天留空”的矛盾 |
| E55 | 未说明缺少钟点到节次依据，直接给出可放班 |

部分完成：R04、E03、E05、E08、E14、E22、E30、E46。主要是追问遗漏已有冲突、能力说明矛盾、低作业量/无需绘画证据不足、名称关系未答、周末偏好未解释、不同老师口碑混用，以及把来源不可用说成未登录。

三门联合排课、整学期周次及部分节次冲突、同课归并、局部课表、默认/指定学期、下午改晚上的多轮偏好，以及只读能力边界均有通过样例。上述结果包含模型及来源波动，不能直接将相较此前的分数变化归因于分支合并。

完整证据保存在应用数据根目录的 `logs/skills-qa-main-merge-20260916-2055/`：`questions.json`、`live-results.jsonl`、`semantic-review.json`、`semantic-review.md`、`run-metadata.json`。元数据记录提交与题集SHA256；真实问答不提交仓库。
