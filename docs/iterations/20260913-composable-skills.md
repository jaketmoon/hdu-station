# 2026-09-13：课程能力可组合

## 需求与边界

用户指出“看好课”不等于“要选课”。保留用户已删除的 E04、E10、E12、E17、E33、E40、E41；基于剩余53组修订，不能恢复旧60组或将全部问题强制变成选课流程。本次不恢复以前整套稳定性迭代。

## 实施日志

1. 定位：单 Skill 要求推荐必查开课；CampusSession 把课表读取当作找课准备，强制完成课程查询；开课后强制 show 工具，限制自由组合。
2. 拆为 course-discovery、offering-verification、timetable-fit 三个原子 Skill，以及组合 course-feasibility。四份短说明共用现有五个只读工具，按本次问题组合，没有另加关键词分类器或业务流水线。
3. 删除跨能力的完成限制。独立课表可直接展示；未消歧候选可以如实显示；校园表格仍由 Go 渲染，社区经验可附在表格前。
4. 时段由程序转换：上午1–5，下午6–9，晚上10–13；可多选，显式节次进一步取交集。只过滤开课时间不需要读本人课表。界面不展示班级号，保留内部身份映射。
5. subagent 排查并实现10个并行对话：请求隔离、同对话互斥、容量释放、逐请求取消、关闭统一等待、设置写入互斥、前端后台完成不覆盖当前输入。
6. 问题集保留53组、58轮，调整独立发现、独立开课、独立课表和追问组合的预期。测试默认10路并发，独立目录保存原始结果，不用重试覆盖。

## 验证记录

- 工具定向单测：时段边界、并集/交集、矛盾条件、只按开课时间过滤不读本人课表、课表独立展示、未匹配候选展示。
- 并发定向验证：`go test -race .`、前端全部26项、桌面/390px并发交互均通过。
- 完整 `make test`、前端检查、构建通过；26项前端测试、18项桌面/390px E2E通过，更新展示替身后相关2项再次通过；四个Skill校验通过。

参考 Eino 官方实现和说明：[ReAct Agent Manual](https://www.cloudwego.io/docs/eino/core_modules/flow_integration_components/react_agent_manual/)。工具循环由模型选择后续读取，最终展示保留现有直接结束机制；没有为本次拆分迁移 Agent 框架。

7. 首轮真实并发诊断：R02 的模型 summary 与正确冲突表相矛盾，撤回新增通用 summary，恢复校园事实仅由 Go 展示；保留独立课表展示和社区摘要。QQ 排队的无上下文 Mutex 也改为可取消入口，保持共享限流。10路社区问答在原串行网络锁下明显堆积，进一步将QQ改为最多3在途读取、全局600ms发起间隔及全局70秒限流冷却，不扩大帖子预算或6分钟期限。第一轮记录保留并标记为中止诊断，最终版本另起目录完整复测。

## 最终全量结果

- 最终目录：应用数据根目录 `logs/skills-qa-20260913-141958`，原始结果、题目快照、逐题评审和完整报告均单独保存。
- 53组、58轮，10路Engine并发，712.05秒：31通过、10部分完成、12失败。没有用重试替换最终轮结果，也不与原60组混算。
- 7组首轮超时：R01、E01、E02、E03、E05、E08、E09。QQ限流与共享等待都有观测，10并发不等于来源可以提供10倍吞吐。
- 5组逻辑失败：E29同课程重复安排、E34无必要的再次确认、E52未读课表却声称冲突、E54未处理矛盾时间、E55忽略钟点依据。
- 部分完成涉及候选丢失/消歧、未请求的课表读取、非最优组合、周次解释及口碑依据不足。R01追问另出现未经用户提出的时间限制。
- E31独立课表、E06/E07/E46/E47独立评价、E50只查开课按时段筛选、E30组合推荐均验证可用。当前不称为稳定版。
- E36自动scope标注过窄：口头简称求正式名称可以使用社区辅助，修正为mixed；仅改评估元数据，未改问句、expected、运行结果，保留原自动标记与修正说明。
- 测试期间产品代码哈希一致，最终仅上述评估元数据修正；新桌面构建已重启。未提交或推送本轮改动。

QQ并发实现参考：[Go官方可取消信号量](https://github.com/golang/sync/blob/master/semaphore/semaphore.go)。实现使用短锁维护共享发起间隔/冷却、有界并发槽和上下文取消；测试使用Go标准库虚拟时间覆盖70秒冷却，不增加产品测试开关。

## 2026-09-15：提交前验证

- 用户要求提交当前改动：四个可组合 Skill、时间段筛选、10个对话并发、QQ可取消限流，以及新账号密集课表题集和两轮迭代日志。
- 执行 `make test frontend-check build`：Go测试、28项前端测试、类型检查、前端及桌面构建均通过。
- 执行 `make e2e`：桌面与390px窄窗口共20项全部通过，包含并行对话、课程结果展示和小红书安全验证兼容检查。
- 本次不重跑真实问答、不修改既有53组测试成绩；已记录的语义问题仍未修复。系统生成的 `build/.DS_Store` 不纳入提交。
