# 2026-09-13：换账号后的密集课表测试

## 本轮范围

用户换成原有课程较多的账号，要求修改部分问题并重新测试。本轮仅修改问题集、说明及测试日志，保留已有 Skill 拆分、10 对话并发和小红书安全验证改动，不改业务实现。

## 调整记录

1. 通过保存的校园认证直接读取当前学期课表和12门官方候选，不让模型猜账号或沿用旧课表。当前为2026-2027学年第一学期，课表读取完整，包含14段占用记录。私有日志只保留数字时段、候选公开课程信息，不保存账号身份、凭证或班级号。
2. 独立将课表和候选展开为（周次、星期、节次）集合计算交集，26个候选班与工具结果一致：12个单门兼容、14个冲突。这是对事实预期的验证，不是问答通过率。
3. 保留53组、58轮及用户删除的7个ID；调整18组：R01–R05、E20、E22、E25–E31、E44、E51–E53。E20仅更新预期，其他调整问法，移除旧账号“只有周三下午有课”等前提。
4. 新增/强化实际场景：周二下午6–7节窄空档、8–9节冲突；周一下午已满但晚上可插；周四前6周空闲而第7–17周部分节次冲突；用户记错空课时以读取结果为准；原课不动、三门联合组合；多轮改成只在周二晚上后两门彼此冲突；单独查看局部课表。
5. 全量问答使用生产 Engine、真实模型和已连接来源，每题独立会话，多轮题继承历史。采用3个测试工作并行，减少上一轮10路社区请求的共享限流干扰；产品10对话上限保持原样。本轮不是10并发性能验收。
6. 运行目录：应用数据根目录 `logs/dense-timetable-qa-20260913-185015`。保留修改前后题目、原始回答、独立事实校验、逐题评审及运行源码哈希，不用重试覆盖首轮成绩。

## 验证与结果

- 全量53组/58轮完成：34 通过、14 部分完成、5 失败；其中调整18组：9 通过、8 部分完成、1 失败。请求错误0组，耗时10.1分钟，3路测试并行。
- 失败：E09纯发现错误调用校园展示、E27三门组合只给两门、E34本学期多余追问、E54矛盾条件忽略、E55自然钟点约束忽略。
- 真实课表的窄空档、后半学期冲突、部分节次冲突和用户错误记忆均正确识别。E52第二个班追问实际读课表并正确回答。
- 部分完成仍涉及模糊候选未确认、摘要与最终表不一致、推荐数量、无依据的零基础推断、额外社区/课表查询、局部忙闲输出不聚焦，以及E51错误改课建议、E53未解析校区内部码展示。详见私有运行目录report.md和逐题review.json。
- QQ、小红书均有真实成功读取；E46两个来源ready，此轮不能证明断连回退路径。测试未重新执行Computer Use或10并发桌面验收。
- 题集结构、26班独立冲突校验、三门组合独立枚举通过；运行期间源码哈希保持一致。只改题目及文档，没有新增业务修复，因此未重复前轮全套UI/构建检查。
- 账号、题目预期、来源和并发数已变化，不直接把本轮比例与旧账号成绩当成同条件提升；无重试覆盖首轮成绩。
